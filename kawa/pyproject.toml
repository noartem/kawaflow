[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kawa"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.12.3",
    "ty>=0.0.1a14",
    "pytest>=8.0.0",
]
